import React from 'react';

const CategoryColumns = (context) => [
    
      {
        Header: '',
        accessor: 'groupnm', // String-based value accessors!
      }
    ] 

export default CategoryColumns;